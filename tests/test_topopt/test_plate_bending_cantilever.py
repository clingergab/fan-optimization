"""fanopt/topopt/plate_bending.py — scaffolded per docs/plan_R11.md §9.2 (vs published benchmark).

Real assertions land alongside the implementation in src/fanopt/topopt/plate_bending.py.
Until then the gate skips so the suite stays green.
"""
from __future__ import annotations

import pytest


def test_placeholder() -> None:
    pytest.skip("Phase 0 scaffold; implementation not yet landed.")
