"""fanopt/bo/multi_fidelity.py — scaffolded per docs/plan_R11.md §6.2.3 (BO sanity check on Branin).

Real assertions land alongside the implementation in src/fanopt/bo/multi_fidelity.py.
Until then the gate skips so the suite stays green.
"""
from __future__ import annotations

import pytest


def test_placeholder() -> None:
    pytest.skip("Phase 0 scaffold; implementation not yet landed.")
