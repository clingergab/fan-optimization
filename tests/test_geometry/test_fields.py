"""fanopt/geometry/fields.py — scaffolded per docs/plan_R11.md §3.2 (Layer 2).

Real assertions land alongside the implementation in src/fanopt/geometry/fields.py.
Until then the gate skips so the suite stays green.
"""
from __future__ import annotations

import pytest


def test_placeholder() -> None:
    pytest.skip("Phase 0 scaffold; implementation not yet landed.")
