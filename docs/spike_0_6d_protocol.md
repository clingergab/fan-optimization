# Spike 0.6d Protocol — Tier-1 added-mass frequency-consistency gate

> **⚠ REDESIGNED 2026-05-15.** The first live Colab run exposed that the
> original 0.6d.1-gating design was unsound (nondimensionalisation
> conflation + a symmetry criterion ill-posed for a net-work fan). The
> gate was rebuilt around a single normalization-invariant test
> (0.6d.2). **Authoritative sources:** `docs/report-final.md` §Phase 0
> Spike 0.6d + `docs/phase_logs/phase_0_signoff.md` **Note 3**. Where
> this protocol's older sections below disagree with Note 3, Note 3
> wins; the load-bearing sections have been rewritten to match.

**Spec reference:** `docs/report-final.md` §Phase 0 Spike 0.6d (added 2026-05-14, redesigned 2026-05-15)
**Motivation reference:** `docs/phase_logs/phase_0_signoff.md` Notes 2 + 3; `docs/phase_logs/spike_0_6c.md` Note 1
**Gate:** Phase 4 launch — `data/spike_0_6d/PASS` required alongside `data/spike_0_6c/PASS`

| Item | Value |
|---|---|
| Spike author | Operator + Claude (2026-05-14; redesigned 2026-05-15) |
| Spike status | Not started |
| V1 scope | Yes — gates Phase 4 launch via **0.6d.2 ONLY**; 0.6d.1 + 0.6d.3 advisory |
| Compute | ~2–4 h Colab Pro CPU (2× short 2D-plate SU2 runs at ω₁, ω₂) |

---

## Why this spike exists

After the 2026-05-14 deferral of Sub-spike 0.6c.2 to Phase 5, the V1 gate set lost its absolute-accuracy evidence channel. Phase 4 would otherwise launch on Tier-1 evaluations with only consistency evidence on SU2's body-in-still-air response at MACH=1e-9. Spike 0.6d provides the independent Phase-0 quantitative check that prevents Phase 4 from burning ~1300 GPU-hours optimizing a Tier-1 objective we can't trust.

**The gate (post-2026-05-15) is a single normalization-invariant falsification test — Sub-spike 0.6d.2:**

Run the 2D thin-plate at TWO pitching frequencies (ω₁, ω₂), same plate / pivot / θ_max. Fourier-project each moment-coefficient trace onto the added-mass (sin φ) basis and recover `I_a = a_sin/(ω²·θ_max)`. `I_a = πρb⁴(1/8+a²)` is a pure geometric/fluid constant — **frequency-independent by construction**. If SU2's MACH=1e-9 + low-Mach-prec unsteady solver is physically faithful, the recovered `I_a` MUST agree between ω₁ and ω₂. The comparison is SU2-vs-SU2 so the fixed `FREESTREAM_PRESS_EQ_ONE` `q_ref` cancels in the ratio — fully normalization-invariant and parameter-free. A frequency-dependent recovered `I_a` falsifies the numerics *before* Phase 4 commits.

0.6d.1 (symmetry/dimensional) and 0.6d.3 (incompressible cross-check) are **advisory** — recorded for Phase 5 step 62.5 but do NOT gate. The strongest cross-check (OpenFOAM `pimpleFoam`, independent codebase) lives in Phase 5 step 62.5.

---

## Sub-spike 0.6d.1 — Symmetry + dimensional-force sanity (ADVISORY, demoted 2026-05-15)

> Recorded for Phase 5; does NOT gate. The procedure below is retained
> for the advisory run; ignore any "GATING"/"Pass criteria" framing —
> per Note 3 this sub-spike's symmetry criterion is ill-posed for a
> net-work fan and its magnitude check is confounded by the `q_ref=1`
> nondimensionalisation. Its output is diagnostic input to Phase 5
> step 62.5, not a Phase-4 blocker.

**Inputs:**
- Existing Cell 8 SU2 history.csv from the 2026-05-14 0.6c.2 run (already on Drive at `gdrive/fan-optimization/spike_0_6c/sub_2_run/history.csv`). NACA 0012 mesh from Cell 6 of the 0.6c notebook.
- One fresh production-Tier-1 run **on that same NACA 0012 mesh** (F4 decision 2026-05-14: reuse the Cell-6 mesh rather than wait for the V1 flat-panel-baseline mesh — that mesh is generated by `mesh_3d_fan.py` at Phase 4 step 46, which is downstream of this gate). The analytic envelope is computed from the NACA 0012 geometry the run is actually performed on (`m_NACA0012 × ω² × r_cm` where mass + r_cm are derived from the airfoil's mesh-defined volume × air density and centroid). The body-in-still-air added-mass scaling is regime-appropriate, not geometry-specific — what matters is that the measured force falls within the envelope for *whatever* geometry the cfg ran on.

**Procedure (`scripts/run_spike_0_6d_1.py`):**
1. Load both history.csv files; parse CL/CD/CFx/CFy/CFz/CMy traces.
2. Compute cycle-averaged F, M over cycles 2–5 (discard cycle 1).
3. Compute dimensional cycle-peak force (un-normalize using FREESTREAM_PRESS_EQ_ONE convention → multiply by reference area × reference dynamic pressure).
4. Compute the analytic added-mass + quadratic-drag envelope from panel mass `m_panel`, peak angular acceleration `θ̈_peak = ω² θ_max`, and radius of mass center `r_cm`. Use F_envelope = m_panel × ω² × r_cm.
5. Run the symmetry + magnitude assertions.

**Pass criteria:**
- (a) **Symmetry:** `|F_cycle_avg| < 0.05 × F_cycle_peak` for the body-frame-symmetric component (CFx for pure pitching about y-axis).
- (b) **Magnitude:** dimensional `F_cycle_peak` within ±1 order of magnitude of the analytic envelope `F_envelope = m × ω² × r_cm`, where mass + r_cm are computed for whichever geometry the cfg actually ran on (NACA 0012 per F4 default; the V1 flat-panel-specific envelope check moves to step 56.5(a) once the Phase-4 mesh exists).

**Fail action:** if (a) fails by a large margin, the simulation has a non-physical mean force bias — investigate solver stationarity (start-up transients into cycles 2-5?) and dual-time inner-iter convergence. If (b) fails, the nondimensionalization is reporting forces 1–4 orders of magnitude off; investigate the FREESTREAM_PRESS_EQ_ONE renormalization path and reference-area definition.

**Output artifacts:**
- `data/spike_0_6d/sub_1_result.json` — `Tier1SymmetryDimensionalResult` payload with all measured values + pass/fail per check
- `data/spike_0_6d/sub_1.PASS` or `data/spike_0_6d/sub_1.FAIL` marker

---

## Sub-spike 0.6d.2 — 2D thin-plate added-mass frequency-consistency (GATING — the sole Phase-4 gate, redesigned 2026-05-15)

**Inputs:**
- One 2D thin-plate mesh — high-aspect-ratio (~50:1) flat plate, chord 1.0 m, circular farfield. Generated by `scripts/gen_benchmark_meshes.py --kind thin_plate_2d`.
- Cfg from `configs/su2/thin_plate_2d_pitching.cfg.j2`, rendered via `render_thin_plate_2d_pitching_cfg`.
- **TWO SU2 runs on the SAME mesh / pivot / θ_max, differing ONLY in pitching frequency** — ω₁ and ω₂ (recommend ω₂ = 2·ω₁).

**Cfg requirements (production-Tier-1-mirror — identical both runs except ω):**
- `MACH_NUMBER = 1e-9` (production Tier-1 lock)
- `REF_DIMENSIONALIZATION = FREESTREAM_PRESS_EQ_ONE` (production Tier-1 fallback)
- `LOW_MACH_PREC = YES`, `TIME_MARCHING = DUAL_TIME_STEPPING-2ND_ORDER`, `GRID_MOVEMENT = RIGID_MOTION`
- `dt = T/200`, 5 cycles; pitching about quarter-chord; C11 sign (ω_y < 0)

**Procedure (`scripts/run_spike_0_6d_2.py`):**
1. Generate the 2D thin-plate mesh once.
2. Render + run SU2 at ω₁ → `history_f1.csv`; render + run SU2 at ω₂ → `history_f2.csv` (same plate/pivot/θ_max).
3. For each run, Fourier-project the moment-coefficient trace onto the added-mass (`sin φ`) and damping (`cos φ`) bases over cycles 2..N (phase reconstructed from the cycle fraction).
4. Recover `I_a(ω) = a_sin / (ω²·θ_max)` for each run — a frequency-independent geometric/fluid constant in whatever fixed nondimensionalisation SU2 emits.
5. **Gate:** `freq_consistency_rel_diff = |I_a(ω₁) − I_a(ω₂)| / mean < 0.25`.
6. Advisory only: compare the recovered `I_a` to the Sedov/Newman closed form `I_a = πρb⁴(1/8+a²)` nondimensionalised under the assumed FREESTREAM_PRESS_EQ_ONE convention. Reported + flagged if off by >2×; **does NOT gate**.

**Pass criterion (GATING):** `freq_consistency_rel_diff < 0.25`. Normalization-invariant (SU2-vs-SU2, fixed `q_ref` cancels), parameter-free.

**Fail action:** a frequency-dependent recovered `I_a` means SU2's MACH=1e-9 + low-Mach-prec unsteady solver does NOT reproduce the correct added-mass frequency signature → the Tier-1 numerics are suspect and **Phase 4 must NOT launch**. Investigate low-Mach preconditioner coefficients, dual-time inner-iter convergence, dt, and consider switching the production unsteady cfg to `INC_NAVIER_STOKES` or escalating to the Phase 5 step-62.5 cross-solver work early.

**Output artifacts:**
- `data/spike_0_6d/sub_2_result.json` — `Tier1AddedMassResult` payload + both per-frequency `AddedMassProjection`s
- `data/spike_0_6d/sub_2.PASS` or `data/spike_0_6d/sub_2.FAIL` marker

---

## Sub-spike 0.6d.3 — SU2 incompressible-mode cross-check (ADVISORY, NOT GATING)

**Inputs:**
- Same NACA 0012 mesh as sub_1 (F4 decision 2026-05-14: reuse the 0.6c Cell-6 NACA 0012 mesh; the V1 flat-panel baseline mesh isn't generated until Phase 4 step 46).
- Duplicate cfg with `SOLVER = INC_NAVIER_STOKES`, same motion + pitching schedule, same boundary conditions.

**Procedure (`scripts/run_spike_0_6d_3.py`):**
1. Render the incompressible variant of the production cfg.
2. Run SU2 for 5 cycles.
3. Parse history.csv; extract cycle-averaged dimensional forces.
4. Compare with compressible-MACH=1e-9 output from sub_1.

**Pass criterion (advisory):** dimensional cycle-averaged forces agree within **±20%**.

**Fail action:** documented as a Phase-5 investigation item but does NOT block Phase 4. Phase 5 step 62.5 (OpenFOAM `pimpleFoam` 3rd-solver cross-check) is the gating answer on this question.

**Output artifacts:**
- `data/spike_0_6d/sub_3_result.json` — `Tier1IncompResult` payload (no marker; advisory)

---

## Aggregator (`scripts/run_spike_0_6d.py`)

Reads `data/spike_0_6d/sub_2_result.json` (REQUIRED — the gate); `--sub-1-json` and `--sub-3-json` optional + advisory. Computes overall pass (post-2026-05-15 redesign):

```
overall_passed = sub_2.freq_consistency_passed   # ONLY
```

Writes:
- `data/spike_0_6d/results.json` — aggregate payload (sub_1/sub_3 recorded if supplied, for Phase 5)
- `data/spike_0_6d/PASS` iff `overall_passed`, else `data/spike_0_6d/FAIL`

Exit codes: 0 = PASS, 1 = FAIL, 2 = sub_2 input missing/malformed.

Exit codes: 0 = PASS, 1 = FAIL, 2 = inputs missing.

---

## Phase 4 launch gate update

`scripts/launch_phase4.py --check` now asserts BOTH markers present:

```python
required_markers = [
    Path("data/spike_0_6c/PASS"),
    Path("data/spike_0_6d/PASS"),
]
missing = [m for m in required_markers if not m.exists()]
if missing:
    print(f"[launch_phase4] missing required gate markers: {missing}")
    return 1
```

Companion test extensions in `tests/test_scripts/test_launch_phase4.py`.

---

## Spec reference vs operational reality (post-2026-05-14)

This protocol is the operational source-of-truth for Spike 0.6d. The plan (`docs/report-final.md` §Phase 0 Spike 0.6d) is the policy source-of-truth: pass criteria, gating semantics, V1/V2 boundary. If the two diverge, the plan wins on policy and the protocol wins on operational mechanics — open an issue to reconcile.

## V2 considerations

If V2 wants quantitative gain claims, the Phase 0.6d → Phase 5 step 62.5 path provides the absolute-accuracy backing. The full chain is: 0.6d.1 (envelope) + 0.6d.2 (closed-form) + 0.6d.3 advisory (Phase 0) → step 62.5 (3-solver cross-check + published reference, Phase 5) → V2 Spike 0.2 (torsion-pendulum hardware ground truth). All three layers are documented in `docs/phase_logs/phase_0_signoff.md` Note 2.

---

## Test coverage plan (must accompany the 0.6d implementation)

Each module + script below has a corresponding test file with the listed coverage gates. **Implementation is not complete until pytest reports all of the following passing alongside the existing 696-test suite.** Mirrors the test-coverage discipline used for Spike 0.6c.

### `src/fanopt/cfd/spike_0_6d.py` → `tests/test_cfd/test_spike_0_6d.py`

Dataclasses + analyzers (no I/O; pure functions over numeric arrays + dicts).

| Test | What it verifies |
|---|---|
| `test_symmetry_passes_on_perfectly_periodic_force_trace` | Synthetic sinusoidal CL/CD with zero mean → `|F_avg| < 0.05 × F_peak` passes |
| `test_symmetry_fails_on_biased_force_trace` | Synthetic trace with non-zero mean (bias_ratio ≈ 2.0) → fails |
| `test_dimensional_envelope_passes_within_order_of_magnitude` | Force magnitude matching analytic envelope within ±1 OOM passes |
| `test_dimensional_envelope_fails_when_off_by_4_orders` | Force magnitude 4 orders too large (CL ~ 10⁶ regime) fails |
| `test_dimensional_envelope_uses_geometry_specific_mass_and_radius` | Envelope `m × ω² × r_cm` recomputed for whatever geometry is supplied |
| `test_added_mass_closed_form_for_2d_plate_at_quarter_chord` | Sedov/Newman closed-form moment matches hand-computed value for known plate dimensions + ω |
| `test_added_mass_closed_form_zero_omega_returns_zero` | Degenerate case (no motion) → zero added-mass moment |
| `test_added_mass_passes_when_su2_within_15pct_of_closed_form` | Synthetic SU2 result + closed-form within ±15% → passes |
| `test_added_mass_fails_outside_15pct` | Synthetic mismatch outside ±15% → fails |
| `test_incompressible_cross_check_passes_within_20pct` | Synthetic comp + incomp forces within ±20% → passes (advisory) |
| `test_incompressible_cross_check_logged_as_advisory_on_fail` | Failure doesn't raise; logged in result dataclass |
| `test_aggregator_pass_requires_sub_1_AND_sub_2_only` | sub_1 PASS + sub_2 PASS + sub_3 FAIL → overall PASS (sub_3 advisory) |
| `test_aggregator_fail_when_either_gating_subspike_fails` | sub_1 FAIL or sub_2 FAIL → overall FAIL |
| `test_aggregator_handles_missing_sub_3_gracefully` | sub_3 absent → overall passes if sub_1 + sub_2 pass |

### `configs/su2/thin_plate_2d_pitching.cfg.j2` + `render_thin_plate_2d_pitching_cfg` → `tests/test_cfd/test_configs.py` (extension)

| Test | What it verifies |
|---|---|
| `test_thin_plate_2d_cfg_mirrors_production_tier1_numerics` | MACH=1e-9 + REF_DIMENSIONALIZATION + DUAL_TIME_STEPPING + LOW_MACH_PREC all match `fan3d_unsteady.cfg.j2` |
| `test_thin_plate_2d_cfg_pitching_about_quarter_chord` | `MOTION_ORIGIN= 0.25 0.0 0.0` |
| `test_thin_plate_2d_cfg_omega_y_negative_per_c11` | C11 sign lock preserved in 2D variant |
| `test_thin_plate_2d_cfg_aspect_ratio_50_to_1` | Mesh dimensions encode the thin-plate geometry |
| `test_thin_plate_2d_renders_with_default_params` | Renderer succeeds with defaults |
| `test_thin_plate_2d_template_file_exists` | The `.cfg.j2` file is on disk |

### `scripts/run_spike_0_6d_1.py` → `tests/test_scripts/test_run_spike_0_6d_1.py`

| Test | What it verifies |
|---|---|
| `test_sub_1_passes_on_synthetic_clean_history` | Synthetic CL/CD history + envelope within bounds → exit 0 + sub_1.PASS marker |
| `test_sub_1_fails_on_biased_history` | Non-zero-mean CL → exit 1 + sub_1.FAIL marker |
| `test_sub_1_writes_result_json_with_all_diagnostics` | Result JSON includes F_cycle_avg, F_cycle_peak, F_envelope, both check booleans |
| `test_sub_1_clears_stale_opposite_marker` | Pre-existing FAIL → after PASS run, FAIL removed |
| `test_sub_1_cfg_only_fallback_when_no_history_csv_provided` | Graceful exit-2 when input missing |

### `scripts/run_spike_0_6d_2.py` → `tests/test_scripts/test_run_spike_0_6d_2.py`

Similar shape to sub_1 — pass/fail/missing-input/marker-clear paths.

### `scripts/run_spike_0_6d_3.py` → `tests/test_scripts/test_run_spike_0_6d_3.py`

| Test | What it verifies |
|---|---|
| `test_sub_3_writes_advisory_result_json` | Result JSON written; no marker (advisory) |
| `test_sub_3_logs_pass_or_fail_in_payload` | Boolean stored; aggregator uses it informationally |

### `scripts/run_spike_0_6d.py` aggregator → `tests/test_scripts/test_run_spike_0_6d.py`

Mirror of the 0.6c aggregator tests but for the dual-gating-plus-advisory pattern.

| Test | What it verifies |
|---|---|
| `test_aggregator_pass_writes_pass_marker` | sub_1+sub_2 PASS → `data/spike_0_6d/PASS` written |
| `test_aggregator_fails_if_sub_1_fails` | sub_1 FAIL → exit 1 + FAIL marker |
| `test_aggregator_fails_if_sub_2_fails` | sub_2 FAIL → exit 1 + FAIL marker |
| `test_aggregator_ignores_sub_3_for_marker_decision` | sub_3 FAIL with sub_1+sub_2 PASS → PASS marker still writes |
| `test_aggregator_payload_includes_sub_3_advisory_result` | sub_3 advisory recorded in results.json |
| `test_aggregator_payload_documents_phase5_step_62_5_target` | Result JSON carries forward-pointer to step 62.5 for absolute-accuracy work |
| `test_aggregator_missing_sub_1_returns_2` | Inputs-missing exit code |
| `test_aggregator_missing_sub_2_returns_2` | Inputs-missing exit code |
| `test_aggregator_clears_stale_opposite_marker` | PASS run clears stale FAIL marker |

### `scripts/launch_phase4.py` dual-gate extension → `tests/test_scripts/test_launch_phase4.py` (extension)

The existing 9 tests must continue passing. Add:

| Test | What it verifies |
|---|---|
| `test_dual_gate_check_passes_when_both_markers_present` | 0.6c PASS + 0.6d PASS → exit 0 |
| `test_dual_gate_check_fails_when_only_0_6c_present` | Only 0.6c PASS → exit 1 with clear "missing 0.6d" message |
| `test_dual_gate_check_fails_when_only_0_6d_present` | Only 0.6d PASS → exit 1 with clear "missing 0.6c" message |
| `test_dual_gate_creates_tag_only_when_both_markers_present` | Tag creation gated on both |
| `test_dual_gate_force_bypasses_both_markers` | `--force` still works the same |

### Audit catalog — `tests/test_audit/test_no_stale_architecture_refs.py`

No new entries needed: 0.6d is purely additive code with no retired identifiers. Re-run the audit gate post-implementation to confirm 0.6d code uses canonical phrasing (no bare CRIT/HIGH/MED labels; round prefixes or absorbed-global labels only).

### Integration / end-to-end (manual operator verification)

These are NOT pytest tests; they live in `docs/phase_checklist.md` Spike-0.6d section and are verified by the operator running on Colab:

| Item | What it verifies |
|---|---|
| Cell 8 history.csv → sub_1 pass on the diagnostic-confirmed body-in-still-air output | sub_1's symmetry check classifies the existing data as the expected regime |
| Fresh production-Tier-1 run on NACA 0012 mesh → sub_1 dimensional check | dimensional envelope holds for real SU2 output |
| 2D thin-plate cfg renders → SU2 runs → sub_2 against Sedov/Newman | added-mass invariant holds |
| Aggregator writes `data/spike_0_6d/PASS` | gate marker present for `launch_phase4.py --check` |
| `launch_phase4.py --check` → exit 0 with both markers | dual-gate works end-to-end |

### Coverage requirement (CLAUDE.md §5.1)

Every new module reports **>80% line coverage**; critical paths + main business-logic branches must be covered. Trivial getters / `__repr__` / pragmas don't need tests.
